# NexusGuard dashboard

The Next.js dashboard for NexusGuard. Setup, architecture and the full feature
tour are in the [repository README](../README.md).

```bash
cp .env.example .env.local
npm install
npm run dev      # http://localhost:3000 — expects the API on :8000
```
